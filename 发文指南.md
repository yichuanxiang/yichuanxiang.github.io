# 发文指南（Cappu 手记）

站点：<https://yichuanxiang.github.io/>
源码：本目录（Hugo + PaperMod），GitHub 仓库 `yichuanxiang/yichuanxiang.github.io` 的 `main` 分支。

工作方式：**把文章放进 `content/posts/`，推送到 GitHub 的 `main` 分支，GitHub Actions 自动构建并发布**（大约 1 分钟）。不需要手动上传 `public/` 目录，也不需要 git 之外的任何操作。

---

## 一、最省事的方式：用现成的三个脚本（双击即可）

| 脚本 | 作用 |
| --- | --- |
| `新建文章.bat` | 问你要个文件名和标题，生成一篇带好格式的新文章，并用记事本打开 |
| `本地预览.bat` | 在自己电脑上预览效果（草稿也能看到），地址 <http://localhost:1313/> |
| `发布博客.bat` | 提交并推送到 GitHub，1 分钟后线上更新 |

日常流程就三步：

1. 双击 **`新建文章.bat`** → 输入文件名（英文/拼音，如 `my-trip`）和标题（可中文）→ 记事本里写正文 → 保存 → 关掉记事本。
2. （可选）双击 **`本地预览.bat`** 看一眼效果，不对就回记事本改，浏览器会自动刷新。
3. 双击 **`发布博客.bat`** → 看到「推送成功」后等 1 分钟，刷新 <https://yichuanxiang.github.io/>。

> 也可以直接复制 `content/posts/文章模板.md`，改名后编辑——效果一样，只是要自己填日期。

---

## 二、文章开头的格式（front matter）

每篇文章开头必须有这一段，用 `---` 包起来：

```toml
---
title: "文章标题"
date: 2026-09-21T20:30:00+08:00
description: "一句话简介，显示在首页列表里"
tags: ["随笔", "折腾"]
draft: false
---
```

- `title`：标题，中文没问题。
- `date`：**必须是过去的或当前的时间**。写未来日期，文章不会出现（Hugo 默认不构建未来文章）。
- `description`：首页列表里那句简介，可不写。
- `tags`：标签，会自动生成标签页，可写多个。
- `draft`：**最容易踩的坑**——`true` 是草稿，**不会被发布**；要上线必须改成 `false`（或删掉这一行）。

正文用 Markdown 写即可：`##` 小标题、`-` 列表、`**加粗**`、`` `代码` ``、``` ```bash ``` 代码块。

## 三、图片、封面

1. 把图片放到 `static/images/` 目录（例如 `static/images/my-photo.jpg`）。
2. 文章里这样引用：`![图片说明](/images/my-photo.jpg)`
3. 想给文章加封面，在 front matter 里加：

```toml
cover:
  image: "/images/my-photo.jpg"
  alt: "图片说明"
  hiddenInSingle: true
```

也可以直接用网图：`![说明](https://example.com/a.jpg)`。

## 四、网址是怎么来的

文章的网址 = 文件名，例如 `content/posts/my-trip.md` → `https://yichuanxiang.github.io/posts/my-trip/`。
所以**文件名建议用英文或拼音**；用中文文件名也能用，但网址会变成一长串 `%E4%B8%AD%E6%96%87` 这样的编码，不好看也不好分享。

改文件名等于改网址，旧链接会失效。已发布的文章尽量别改名。

---

## 五、常见问题

**推送上去了，网站没变？**
1. 先确认 `draft: false`、`date` 不是未来时间。
2. 去 <https://github.com/yichuanxiang/yichuanxiang.github.io/actions> 看最近一次运行是否绿勾（约 30–60 秒）。
3. 浏览器按 `Ctrl+F5` 强刷，GitHub Pages 有缓存。

**`发布博客.bat` 报错？**
`git push` 需要登录 GitHub。第一次可能会弹出浏览器登录窗口，登录账号 `yichuanxiang` 即可；报错内容直接截图发给 AI。

**想本地看看但 `本地预览.bat` 说找不到 hugo？**
`本地预览.bat` 默认用 `..\.tools\hugo-0.160.1\bin\hugo.exe`（已存在，版本 0.160.1，和线上构建一致）。若那个目录被挪走，就需要单独安装 Hugo Extended 并加入 PATH。

**在别的电脑/平板上想发文？**
两种办法：① 把那台电脑也配好 git + Hugo 再走上面的流程；② 直接在 GitHub 网页上操作：打开 <https://github.com/yichuanxiang/yichuanxiang.github.io> → 进入 `content/posts/` → **Add file → Create new file** → 文件名写 `xxx.md` → 粘贴文章内容（含开头的 `---` 部分）→ 底部 **Commit changes**。提交后同样会自动发布。

---

## 六、给 AI 的一句话说明

以后想让 AI 帮忙发文，可以直接说：

> 帮我在 `D:\aishengtu\cappu-blog-pages\content\posts\` 新建一篇 `xxx.md`，标题是「……」，写完提交并推送到 main 分支。

（Hugo 可执行文件：`D:\aishengtu\.tools\hugo-0.160.1\bin\hugo.exe`；本地构建验证：在该目录运行 `hugo --quiet`。）
